import { createContext } from "react";

export const ThemeContext = createContext();
export const CityListContext = createContext([]);
export const UserLat = createContext([]);
export const UserLon = createContext([]);
